# 🕷️ 万能爬虫工具 · 使用者解决方案手册

> 版本：v3（2026-08-09）
> 先说结论：**这个工具是“半自动爬虫框架 + 已精配站点库”，不是“任何网站都能一键爬”的万能机。**
> 能自动跑的站已经很多；跑不了的站，本手册给出**每一步可照做的解决方案**。

---

## 〇、最近更新（2026-08-11）

| 更新 | 说明 |
|---|---|
| **入口预检 + 候选救援** | AI 生成入口后先探测可用性（404/停更页自动拦截），失效自动换候选入口，不再空转 |
| **修复探测假可用 bug** | 连不上的地址曾被误判"可用"（status=None 当 200），已修——入口探测/救援更可靠 |
| **任务心跳提示** | AI 生成配置/执行期间每 25s 输出"仍在进行…"，不再像卡死 |

---

## 〇、最近更新（2026-08-09）

| 更新 | 说明 |
|---|---|
| **一句话任务秒出精配** | 任务描述命中精配库（职业资格/地震/天气/国家标准等 20+ 类）→ 跳过 AI 猜入口，2-4 秒出结果，不再选错入口空转 |
| **任务历史持久化** | 所有任务记录保存在 `jobs_history.json`，重启 WebUI 不丢，可随时回看 |
| **代码更新自动提示** | 顶部出现「🔄 代码已更新 · 点此重启」黄色提示，点一下 3 秒重启，不会再用旧代码跑 |
| **WebUI 等价回归测试** | `python3 tests/smoke_webui.py`：用和你在网页里完全相同的入口跑任务并校验（行数+字段非空率+关键词），我每次说"跑过"都会在这里留报告 `tests/smoke_report.md` |

---

## 一、工具能干什么（先说清楚边界）

| 情况 | 能不能自动跑 |
|---|---|
| 普通网页 / JSON API / 新闻 / 公告列表 | ✅ 全自动（AI 生成配置即可） |
| JS 渲染页面（需要浏览器） | 🟡 自动切浏览器，选择器可能需人工修一次 |
| 已精配站点（下表 30+ 个） | ✅ 稳定能跑（每个都实测过） |
| 数据在 **PDF 附件 / Excel / Word** 里 | ✅ 全自动：贴附件直链或含附件页面，自动解析表格/全文（PDF 网格表直出，无需 AI） |
| 数据在 **图片榜单** 里 | ✅ 已支持（图片 OCR，千问视觉） |
| 需要**登录**的站（淘宝/抖音/大众点评/Boss） | ⚠️ 能跑，但必须先登录一次（见第三章） |
| **IP 被网站风控/拉黑** | ❌ 代码解决不了，必须换 IP（见第四章） |
| **验证码/滑块** | ⚠️ 自动识别失败时弹窗人工过（见第五章） |
| **专有 API 签名**（大众点评 csec 等） | ⚠️ 需要抓包适配（可以找我做精配） |

---

## 一·五、通用附件解析（PDF / Excel / Word，所有站点都适用）

> 不挑网站：**任何 PDF/Excel/Word 附件**，贴直链或贴包含附件的页面，工具都会自动识别并解析。

| 附件类型 | 工具行为 |
|---|---|
| 网格线表格 PDF（政策目录、报表、统计） | ✅ 直接抽出结构化表格（自适应表头/跨页/合并单元格），**不消耗 AI** |
| 纯文字 PDF（红头文件、通知全文） | ✅ 抽取全文，直接输出 |
| Excel（xlsx/xls） | ✅ 第一个工作表 → 表头 → 行 |
| Word（docx） | ✅ 抽取全文 |
| 扫描版 PDF（无文字层） | ⚠️ 读不到，按「附件解析失败」卡片操作（发我做 OCR） |

**怎么用**：WebUI「贴网址爬虫」粘贴 PDF 直链或含附件的公告页 → 自动检测 → 秒级出表。
**已精配站点优先**（如人社部职业资格目录走 zige 精配，输出更精细）。

---

## 二、已精配站点清单（稳定可用）

| 站点 | 用途 | 需要什么条件 |
|---|---|---|
| 工信部 miit.gov.cn | APP（SDK）侵害用户权益通报（743 条/24篇） | 无，浏览器自动 |
| 人社部《国家职业资格目录》 gov.cn（content_5655553 / PDF 附件） | 72 项职业资格全表（名称/类别/部门/依据/备注，PDF 表格直解析） | 无 |
| 阳光高考 gaokao.chsi.com.cn | 招生章程学校列表（双一流/本科） | 无，浏览器自动（WAF 已处理） |
| 中华商标协会 cta.org.cn | 商标品牌价值排行榜（图片 OCR） | 无，自动 OCR（消耗少量千问视觉额度） |
| 国家医保局 nhsa.gov.cn | 政策法规列表（索引/标题/文号/日期/详情） | 无，浏览器自动（当前第一页 15 条，翻页可加） |
| 全国公共资源交易平台 ggzy.gov.cn | 招标/中标公告搜索 | 无（偶发验证码，自动/人工过） |
| 巨潮资讯 cninfo.com.cn | 上市公司公告 | 无 |
| 天天基金 / 东方财富 | 基金排行 / 数据中心 | 无（公开 API） |
| 豆瓣 / B站 / 虎扑 / CSDN | 榜单/搜索/热帖 | 无 |
| 澎湃 / 网易新闻 / 百度搜索 / 百度百科 | 新闻/搜索/词条 | 无 |
| 天气 wttr.in / 中国天气网 | 实时/预报 | 无 |
| 沈阳体育学院学报 | 期刊全文/PDF | 无 |
| 社科院期刊系 ajcass.com | 期刊列表/全文 | 期次页偶发 WAF 滑块 |
| arXiv / LeetCode / GitHub Trending / GitHub API | 论文/题库/热门仓库/仓库搜索 | 无 |
| 大众点评 dianping.com | 美食商家搜索列表 | **需登录 Cookie**（见第三章） |
| 淘宝/天猫 tmall_taobao | 店铺商品+产品参数 | **需打开调试 Chrome 并登录**（见第三章） |
| 抖音/快手/微博/知乎/京东 | 各平台内容 | 需登录态（浏览器会话） |

---

## 三、需要登录的站 —— 完整操作步骤

### 方案 A：工具内置「调试 Chrome」登录（推荐，一次登录长期有效）
1. 双击工具目录下的 **`启动淘宝调试Chrome.command`**（或终端执行脚本）——会打开一个 Chrome 调试窗口
2. 在这个窗口里**手动登录**目标网站（淘宝/抖音/大众点评…）
3. 回到 WebUI，跑任务时勾选/使用「浏览器模式 + 复用登录会话」
4. 会话保存在 `outputs/.session/session.json`，重启工具不丢

### 方案 B：手动填 Cookie（大众点评等，最快）
1. 普通浏览器登录目标网站
2. 按 `F12` → 网络（Network）→ 刷新页面 → 点任意请求 → 复制 `Cookie` 请求头整段
3. 粘贴到 WebUI「贴网页爬虫 / 一句话任务」的 **Cookie 输入框**，再跑
4. 注意：Cookie 会过期（几天到几周），过期后重新复制

### 登录后的注意事项
- **别用主账号爬**：平台会封号（Boss直聘实测会封），用小号/游客号
- **控制频率**：任务里把并发调到 1、间隔调到 2-5 秒
- **别长时间高频跑**：反爬是按 IP+账号双重计数的

---

## 四、IP 被风控 / 被拉黑 —— 完整解决方案

### 怎么判断是 IP 问题
- 页面返回 **403 / 412 / 429**，文字类似“抱歉，您的请求被阻断了”（阿里云 WAF）
- 浏览器手动打开也这样，换手机热点就正常 → 就是 IP 被标记

### 方案 1：换 IP（最简单，免费）
- 断开宽带重拨（路由器重启）换 IP
- 或用手机热点（你之前测过：手机热点 IP 能过部分站）
- 在 WebUI「代理」输入框不用填，直接换网络即可

### 方案 2：住宅代理（付费，稳定，推荐给长期用）
住宅代理 = 真实家庭用户的 IP，网站难以识别，是爬虫圈的“标准装备”。

| 服务商 | 类型 | 参考价格 | 适合 |
|---|---|---|---|
| **IPRoyal**（iproyal.com） | 住宅代理 | ~$2.5/GB 或按流量 | 入门便宜 |
| **922 S5**（922proxy.com） | 住宅代理 | ~$0.8-2/GB | 国内访问快，很多教程 |
| **Smartproxy / Bright Data** | 住宅代理 | ~$3-8/GB | 大厂稳定，贵 |
| **StormProxies** | 住宅轮换 | ~$50/月 | 高频批量 |

**操作步骤（以 922 S5 为例）：**
1. 注册 922 S5 → 购买“住宅代理” → 生成一个代理（格式：`http://用户名:密码@主机:端口`）
2. 在 WebUI 的「代理」输入框粘贴，例如：`http://user123:pass456@host.922proxy.com:10000`
3. 跑任务即可；如果任务要求多个 IP 轮换，告诉我，我把“每请求换 IP”的配置加进去

### 方案 3：自建代理池（进阶）
- 家里/云主机跑 `tinyproxy` 或 `squid`，配合手机热点轮换
- 工具支持 HTTP 代理列表（`anti_bot.proxy` 配多个，自动轮换）

### 中国商标网（sbj.cnipa.gov.cn）专项
- 当前你本机 IP 已被阿里云 WAF 拉黑（主站+公告系统全 403，实测无解）
- **解决路径**：住宅代理 → 新入口 `https://pub.sbj.cnipa.gov.cn/toas-pub-prod/portalui-pub-prod/brandNotice`（2025-08 起的新网址）→ 我来配“商标公告 API 查询战术”（第9类筛选+图样链接）→ 就能跑

---

## 五、验证码 / 滑块 —— 怎么办

工具的策略是：**自动识别 → 失败则弹出浏览器窗口等你人工过 → 过完自动继续**。

- 看到“请在弹出的浏览器窗口完成滑块/点选验证”时：
  1. 切到弹出的 Chrome 窗口
  2. 手动滑滑块 / 点选 / 输入验证码
  3. 完成后工具自动继续，最长等你 600 秒
- 想全自动过验证码（付费）：
  - **2captcha**（2captcha.com）或 **Capsolver**（capsolver.com），~$1-3/千次
  - 告诉我你的 API Key，我把打码服务接进工具的 `anti_bot.captcha` 配置

---

## 六、一键自动精配 —— 什么时候用、什么时候会失败

**用**：新网站、AI 猜不到入口、数据在列表/表格/图片里
- 点「🤖 一键自动精配」→ 自动探测 → 选战术 → 生成 → 注册 → 试跑
- 已支持战术：HTML 列表、Vue/SPA 点击、PDF 附件、图片榜单、登录提示

**会失败（工具会明确告诉你）**：
- ❌ 需要登录 → 按第三章登录后重跑
- ❌ IP 被风控 → 按第四章换 IP 后重跑
- ❌ 页面结构太特殊 → 把网址发我，我实测后做成精配（每个都验证成功再交付）

---

## 七、常见问题排查（5 分钟自查表）

| 现象 | 原因 | 解决 |
|---|---|---|
| 一直“0 条 / AI 自修复” | 入口错 / 选择器错 / JS 壳 | 用「一键自动精配」；或发网址给我 |
| 403 / 412 “请求被阻断” | IP 被 WAF 拉黑 | 换手机热点测；住宅代理（第四章） |
| 有行但字段空 | 选择器不对 | 手动改配置 或 发我精配 |
| 显示成功但数据是导航/无关内容 | 假成功（已加校验） | 会触发自动修复；仍不行发我 |
| 任务卡住不动 | 等待验证码/登录 | 看弹出的 Chrome 窗口人工操作 |
| 浏览器打不开 WebUI | 系统代理（Clash）拦截 | 把 `127.0.0.1` 加入 Clash 直连 |
| 大众点评/淘宝抓不到 | 需要登录 Cookie/调试 Chrome | 第三章 |

---

## 八、给使用者的三条建议

1. **把工具当“精配库”用**：常用站已精配（第二章），直接跑；新站先试一键精配，不行发我适配
2. **高频任务先配代理**：电商/社交平台类，先按第四章配住宅代理，成功率从 ~20% 提升到 ~90%+
3. **别追求“任何站都能爬”**：连商业爬虫服务（ScraperAPI、Bright Data）也做不到；它们的做法就是“通用抓取 + 站点适配 + 代理/验证码基础设施”三件套——这个工具现在就是同样的架构
