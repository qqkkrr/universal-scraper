# 万能爬虫工具 · 使用说明书（新手友好版）

> 一个**配置驱动 + 插件化**的 Python 爬虫框架：把"抓网页"这件事变成"写一个 JSON 配置"。
> 本说明书面向**没写过爬虫的代码新手**，从零开始教你用它抓数据。

---

## 1. 这是什么？能干什么？

爬虫 = **自动访问网页，把网页里的内容整理成表格/数据**。

这个工具把爬虫拆成 4 个可配置的部分：

```
取数据（HTTP / 浏览器 / 桥）→ 解析（HTML / JSON / AI）→ 清洗（过滤/去重）→ 存结果（JSON/CSV/Excel/SQLite）
```

你不需要写爬虫代码，只需要：
1. 告诉它**访问哪个网址**
2. 告诉它**数据在哪**（CSS 选择器 / JSON 路径）
3. 运行，得到 `xxx.json` / `xxx.csv` / `xxx.xlsx`

真实案例：用它抓过 **books.toscrape.com 100 本书**、**GitHub 搜索 10 个仓库**、**Hacker News 30 条**、**国务院政策图解 5 条**。

---

## 2. 环境准备（一次性）

需要三个东西（都是免费的）：
| 组件 | 作用 | 安装 |
|---|---|---|
| Python 3.10+ | 运行框架 | https://www.python.org/downloads/ |
| Node.js 18+ | 浏览器自动化（抓 JS 页面时用） | https://nodejs.org/ |
| Playwright 浏览器内核 | 无头浏览器 | 见下 |

```bash
# 在项目目录里
python3 -m pip install requests lxml openpyxl   # 可选依赖（没有也能跑，会自动降级）
npm init -y 2>/dev/null; npm install playwright   # 需要浏览器渲染时才装
python3 -m playwright install chromium            # 下载浏览器内核（约 100MB）
```

> 工具本身**零第三方依赖也能跑**（纯 Python 标准库），装依赖是为了更快/更强。

---

## 2.4 🤖 自动抓取（只说任务，AI 全自动）

在网页界面切到 **「🤖 自动抓取」**，输入一句话任务，例如：
```
抓取 https://quotes.toscrape.com/ 的名言、作者和标签，翻 2 页
抓取 https://books.toscrape.com/ 的书名和价格
抓取 https://jsonplaceholder.typicode.com/posts 的标题和正文，只要 2025-01-01 到 2025-01-03 的
```
点「开始自动抓取」，工具会：
1. **先探测入口页结构**：自动抓一次入口页，提炼"高频 class / 候选列表行 / 翻页链接模式 / JSON 字段"摘要（不把整页源码塞给 AI，省 token、首轮配置更准）
2. **AI 生成任务配置**（自动选 HTTP/浏览器、写选择器、配翻页、配日期过滤；分页链接自动锚定，防止 `/tag/xxx/page/1/` 这类同构 URL 把队列撑爆）
3. **自动运行**
4. **失败自动修复**：0 条、报错、或"全是空壳字段"时，把日志喂回 AI 修正配置再跑（最多 2 轮）

> 选择器兼容性：写 `.text` 或 `.text::text` 都行（工具自动剥离 `::text`/`::attr(href)` 伪元素），
> 就算 AI 写出带伪元素的选择器也不会抓空。

命令行同样支持：
```bash
python3 -m universal_scraper.cli auto "抓取 https://quotes.toscrape.com 的名言、作者和标签，翻 2 页"
```

> 去重：同一次任务里可加 `{"type":"dedup_content","fields":["标题","正文"]}` 按内容哈希去重；
> 跨多次运行可开 `"incremental": {"enabled": true, "key": "content_hash"}`，重复内容自动跳过（对标 Browsertrix 内容哈希去重）。

> ⚠️ 诚实说明：AI 自动层能处理**绝大多数公开网页/API**（已实测名言网、书架、JSON 接口、GitHub、HN）。
> 但**无法自动**的硬边界：需要登录（会弹出浏览器让你登录一次）、验证码、强 WAF 网站、
> 以及"站点本身翻不到的历史数据"（比如小红书按热度排序翻不到 2025 年 1 月的老笔记）。

---

## 2.5 可视化界面（零代码推荐）

工具自带一个**本地网页版界面**，不用记命令，浏览器里点点点就能抓数据：

```bash
python3 -m universal_scraper.cli webui
# 然后打开浏览器访问 http://127.0.0.1:8642
```

界面上有 5 个页签：
- **⚡ 快速抓取**：贴网址 → 选方式 → 点"抓取"，立刻看到内容/表格/外链
- **▶️ 运行任务**：下拉选一个现成任务 → 点"运行"，看条目数、日志、数据预览
- **📊 查看结果**：选 outputs/ 里的结果文件，表格展示
- **📁 任务列表**：看有哪些任务包
- **❓ 帮助**：三步入门

> 零依赖（Python 标准库），局域网内可用：`webui --host 0.0.0.0 --port 8642`。
> 适合新手先用界面摸清套路，再回去看命令行进阶。

### 实战：用网页界面抓小红书（#淮安美食，2025-01-01~2025-01-03）

工具已内置两个小红书配置（在网页界面「▶️ 运行任务」下拉里能看到）：

1. **先登录（只需一次）**：选 `xhs_login` → 点运行 → 会自动弹出真实浏览器窗口
   → 扫码/手机号登录小红书 → 登录成功后会话自动保存，窗口关闭。
2. **再抓取**：选 `xhs_huai_an_food` → 点运行 → 自动打开
   `#淮安美食` 搜索结果页并滚动加载 → 捕获接口数据 → **自动只保留
   2025-01-01 ~ 2025-01-03 的笔记**（已内置日期区间过滤）。
3. **看结果**：切到「📊 查看结果」→ 选 `xhs_huai_an_food.json` → 表格展示。

注意事项：
- 小红书反爬很强：遇到验证码会弹窗/提示，按提示处理即可；登录态保存在
  `outputs/.session/xhs_huai_an.json`，之后不用再登录。
- 搜索结果默认按热度排序，**2025 年 1 月的老笔记可能翻不到**：
  如果结果为 0，可把配置里 `scroll_count` 从 60 调大（比如 200），
  或在关键词里加年份再搜。
- 结果字段：id / title / time（Unix 时间戳）/ url / 点赞数 等，导出 Excel 后可再筛选。

---

## 3. 三分钟极速上手

```bash
# ① 一键抓一个网页，输出干净 Markdown
python3 -m universal_scraper.cli fetch "https://books.toscrape.com/"

# ② 一键递归爬一个网站（自动翻页、自动提取标题和正文）
python3 -m universal_scraper.cli crawl "https://quotes.toscrape.com/" --max 20

# ③ 运行一个"任务包"（推荐，最强大）
python3 -m universal_scraper.cli run --task tasks/demo_recursive
```

跑完看 `outputs/` 文件夹，里面有 `json / csv / xlsx` 三种格式的结果。

---

## 4. 核心概念：任务包（Task）

任务包 = 一个文件夹，里面有一个 `config.json`（配置）和可选的 `modules/`（自定义代码）。

```
tasks/你的任务名/
├── config.json        # 告诉爬虫：抓哪、怎么抓、存哪
└── modules/           # 可选：复杂逻辑才需要写代码
```

生成一个任务包模板：
```bash
python3 -m universal_scraper.cli scaffold --type task --name my_task --out tasks/my_task
```

### config.json 字段详解（新手必看）

```jsonc
{
  "name": "my_task",                    // 任务名（会出现在输出文件名里）
  "vars": {"keyword": "数据中心"},       // 变量，可以在 URL 里用 {{keyword}} 替换

  "start_urls": ["https://site.com/list"],   // 入口网址（爬虫从这开始）

  "queue": {
    "max_depth": 5,          // 最多跟几层链接（0=只看入口页）
    "max_requests": 500,     // 最多抓多少页（防止失控）
    "max_concurrency": 4     // 同时几个请求（越大越快，小心被封）
  },

  "source": {
    "type": "http",          // 取数方式：http / browser（JS页面）/ bridge（专用桥）
    "query": {"page": "1"}   // 固定的 URL 参数
  },

  "rules": [                 // 路由：哪个网址用哪个解析器
    {"match": "contains", "pattern": "/list",   "parser": "list"},
    {"match": "regex",   "pattern": "/detail/\\d+", "parser": "detail"}
  ],

  "parsers": {              // 解析器：从网页里提取数据
    "list": {
      "type": "html",                        // html 页面
      "row_css": "tr.item",                  // 每一行数据的选择器
      "fields": {                            // 要提取的字段
        "title": {"css": "td.t"},            //   标题 = td.t 的文本
        "url":   {"css": "a", "attr": "href"} //   链接 = a 的 href 属性
      },
      "extract_links": {"allow": "/detail/\\d+"}  // 把符合的链接继续抓
    },
    "detail": {
      "type": "html",
      "fields": {"body": {"css": ".content", "limit": 5000}}
    }
  },

  "pipelines": [            // 数据清洗流水线
    {"type": "filter", "field": "title", "op": "non_empty"},  // 去掉没标题的
    {"type": "dedup",  "key": "url"}                          // 按 url 去重
  ],

  "storage": {"type": "jsonl", "name": "my_task"},  // 存 jsonl（最快）
  "output":  {"dir": "outputs", "base_name": "my_task"},  // 导出 json/csv/xlsx

  "anti_bot": {             // 反爬设置
    "min_interval": 1.0,    // 每个域名至少间隔 1 秒（礼貌）
    "max_retries": 3        // 失败重试次数
  }
}
```

> **CSS 选择器是什么？** 网页由 HTML 组成，选择器就是"定位到某个元素"的地址。
> 在浏览器里右键网页元素 → 检查 → 就能看到 `class="xxx"`，写 `.xxx` 即可选中。
> 例：`<span class="price">£10</span>` → `{"price": {"css": "span.price"}}`

---

## 5. 一步一步：适配一个新网站（以名言网为例）

**目标**：抓 quotes.toscrape.com 的名言、作者、标签，翻 2 页。

**第 1 步：用 `fetch` 看网页结构**
```bash
python3 -m universal_scraper.cli fetch "https://quotes.toscrape.com/" --selector ".quote"
```
看到每条名言在 `<div class="quote">` 里。

**第 2 步：写任务包**
```bash
python3 -m universal_scraper.cli scaffold --type task --name my_quotes --out tasks/my_quotes
```
编辑 `tasks/my_quotes/config.json`：
```jsonc
{
  "name": "my_quotes",
  "start_urls": ["https://quotes.toscrape.com/"],
  "queue": {"max_depth": 3, "max_requests": 10, "max_concurrency": 2},
  "source": {"type": "http"},
  "rules": [{"match": "contains", "pattern": "/", "parser": "js"}],
  "parsers": {
    "js": {
      "type": "html",
      "row_css": ".quote",
      "fields": {
        "title":  {"css": "span.text"},
        "author": {"css": "small.author"},
        "tags":   {"css": "a.tag"}
      },
      "extract_links": {"allow": "/page/\\d+", "same_domain": true}
    }
  },
  "pipelines": [{"type": "filter", "field": "title", "op": "non_empty"}],
  "storage": {"type": "jsonl", "name": "my_quotes"},
  "output": {"dir": "outputs", "base_name": "my_quotes"},
  "anti_bot": {"min_interval": 0.5, "max_retries": 2}
}
```

**第 3 步：运行并看结果**
```bash
python3 -m universal_scraper.cli run --task tasks/my_quotes
cat outputs/items/my_quotes.jsonl    # 每行一条数据
open outputs/my_quotes.xlsx          # Excel 打开
```

完成！抓新网站 = 改选择器 + 改网址，通常 5 分钟。

---

## 6. 命令大全

| 命令 | 作用 | 示例 |
|---|---|---|
| `fetch <url>` | 一键抓单个网页 | `fetch "https://x.com" --browser --article` |
| `crawl <url>` | 一键递归爬站 | `crawl "https://x.com" --depth 3 --allow "/docs/"` |
| `run --task <目录>` | 运行任务包 | `run --task tasks/my_quotes` |
| `run --config <json>` | 运行 v2 配置 | `run --config configs/ggzy_datacenter_2025-01.json` |
| `run --resume` | 断点续跑 | `run --task t --resume` |
| `run --dry-run` | 只校验不抓 | `run --task t --dry-run` |
| `run --url <url>` | 换入口网址 | `run --task t --url "https://..."` |
| `run --var k=v` | 覆盖变量 | `run --task t --var keyword=数据中心` |
| `jobs --file a.json` | 批量跑多个任务 | `jobs --file jobs.json` |
| `schedule --task t` | 定时循环跑 | `schedule --task t --every 60` |
| `monitor --task t` | 监控网页变化 | `monitor --task t --times 5` |
| `validate --config f` | 校验配置 | `validate --config configs/x.json` |
| `scaffold --type task` | 生成模板 | `scaffold --type task --name t` |
| `list` | 列出配置 | `list` |

常用参数速记：
- `--browser`：用浏览器渲染（JS 页面、需要点击的页面）
- `--limit N`：只处理前 N 条（先小规模试跑）
- `--allow / --deny`：只抓/不抓匹配正则的链接
- `--proxy http://127.0.0.1:7890`：走代理
- `--json`：fetch 输出纯 JSON（方便程序处理）

---

## 7. 进阶能力（新手知道"有"就行，用的时候再看）

| 能力 | 配置/命令 | 说明 |
|---|---|---|
| **JS 页面** | `"source": {"type": "browser"}` | 自动渲染 Vue/React 页面 |
| **动作链** | `"actions": [{"type":"click","selector":"#more"}]` | 模拟点击"加载更多"、输入搜索、下拉选择 |
| **登录态** | 浏览器池 + `session.json` | 登录一次，后续请求带 Cookie |
| **反检测** | `"stealth": true` + `"remove_overlays": true` | 伪装真人、自动关弹窗 |
| **代理池** | `"proxies": ["http://u:p@host:1", "..."]` | 多代理轮换、失败自动换 |
| **TLS 伪装** | `"http_backend": "auto"` | 装了 curl_cffi 自动伪装浏览器指纹 |
| **JSON 分页** | `"type": "json_paged"` | page/offset/next 三种翻页 |
| **robots.txt** | `"respect_robots": true` | 自动遵守网站爬虫协议 |
| **增量去重** | `"incremental": {"enabled": true, "key": "id"}` | 第二次运行跳过已抓 |
| **断点续跑** | `run --resume` | 中断后从断点继续 |
| **多存储** | `"storage": {"type":"multi", "backends":[...]}` | 一次写 jsonl+csv+sqlite |
| **监控变化** | `monitor` 命令 | 新增/消失/变更三条 diff |
| **插件** | `tasks/x/modules/fetcher.py` 等 | 完全自定义取数/解析/存储 |

---

## 8. 常见问题 FAQ

**Q：运行 `webui` 后浏览器打不开？**
先确认终端出现 `✅ 服务已启动`；然后：
1. 系统代理（Clash 等）请把 `127.0.0.1` 加入直连/绕过（代理会拦截本机地址导致转圈）
2. 换端口重试：`python3 -m universal_scraper.cli webui --port 8643`
3. 换浏览器（Chrome/Edge/Safari）；或手机同局域网访问：`webui --host 0.0.0.0` 后用电脑局域网 IP 打开
4. 不要重复启动两个实例（端口被占用会提示换端口）

**Q：抓到的中文乱码？**
不会。工具会自动识别 gbk/utf-8 编码（header → BOM → meta 三级探测）。

**Q：页面要登录才能看？**
先把登录态存下来：用浏览器打开页面手动登录一次 → 工具会在 `outputs/.session/` 保存会话，之后自动带上。

**Q：被封 IP / 429？**
工具会自动：指数退避重试、按 `Retry-After` 等待、代理轮换、限速。你只需调大 `min_interval` 或加代理。

**Q：抓太快被要求验证码？**
四级方案：自动 OCR（ddddocr）→ 滑块识别（OpenCV）→ 付费代解（2captcha）→ 人工输入（把验证码图存下来你输一次）。

**Q：怎么知道抓的对不对？**
先 `--limit 5` 小跑，看 `outputs/items/xxx.jsonl` 前几行；不对就调 `row_css`/`fields`。

**Q：出错怎么看？**
每次运行有日志 `outputs/.run_任务名.log`；用 `run --log-file /tmp/a.log` 可指定位置。

**Q：只想抓 API（JSON）？**
`"parsers": {"js": {"type":"json", "records_path":"data.records", "fields":{...}}}`，或直接 `fetch "https://api..." --json`。

---

## 9. 怎么跑测试 / 验证没改坏

```bash
# 自动化回归套件（自动起本地模拟站，无需联网）
python3 tests/run_tests.py        # 第 1 批 10 项
python3 tests/run_tests2.py       # 第 2 批 10 项
# ... 一直到 run_tests32.py
# 最终严苛测试（含 7 个真实站点）
python3 tests/run_final.py
```
结果：32+1 套件，共 475 项测试全部通过（真实站点 + 高强度场景）。

---

## 10. 目录结构

```
universal-scraper/
├── universal_scraper/     # 核心代码（26 个模块）
│   ├── cli.py             # 命令行入口
│   ├── engine_v3.py       # v3 引擎（任务包）
│   ├── engine.py          # v2 引擎（配置）
│   ├── core.py            # HTTP 客户端 / 导出
│   ├── modules/           # 内置解析器/存储/中间件
│   └── ...
├── scripts/               # Node 浏览器桥（Playwright）
├── tasks/                 # 17 个示例任务包（从简单到高级）
├── real_tasks/            # 15 个真实网站任务
├── configs/               # v2 配置（含小红书、公共资源交易平台）
├── tests/                 # 33 个测试套件（475 项测试）
├── outputs/               # 结果输出（json/csv/xlsx/jsonl）
├── 使用说明书.md           # 本文件
└── README.md              # 开发者文档（各轮强化记录）
```

---

## 最后

记住三句话：
1. **先 `fetch` 看结构，再写 `config.json`** —— 5 分钟搞定一个新网站。
2. **先小跑（`--limit 5`），再全量** —— 出错成本最低。
3. **复杂场景交给 `modules/`** —— 插件机制让你想怎么抓就怎么抓。

祝你抓取顺利！🎉
